CREATE TABLE PET (
    ID                  SERIAL               PRIMARY KEY   NOT NULL,
    NAME                VARCHAR                            NOT NULL,
    TYPE                VARCHAR                            NOT NULL,
    COST                DECIMAL                            NOT NULL
);
